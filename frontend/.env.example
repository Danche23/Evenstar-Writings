# 复制为 .env.development.local 并填入真实值（该文件已被 .gitignore 忽略，不会提交）

# 后端 API 前缀：开发期留空，走 vite proxy /api -> http://localhost:8080
VITE_API_BASE=

# 阿里云验证码 2.0（与后端 configs/config.yaml 的 captcha 段保持一致，非密钥，仅前端初始化用）
VITE_CAPTCHA_PREFIX=
VITE_CAPTCHA_SCENE_ID=
